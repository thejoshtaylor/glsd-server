---
phase: 14-project-management
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - frontend/src/types/api.ts
  - frontend/src/components/projects/ProjectManager.tsx
  - frontend/src/components/projects/ConnectProjectDialog.tsx
  - frontend/src/components/projects/CloneProjectDialog.tsx
  - frontend/src/components/projects/BootstrapProjectDialog.tsx
  - frontend/src/components/execute/ExecuteForm.tsx
  - frontend/src/routes/dashboard/$nodeId.tsx
autonomous: false

requirements: [PROJ-01, PROJ-02, PROJ-03, PROJ-04]

must_haves:
  truths:
    - "User can see a list of registered projects for a node in the ProjectManager panel"
    - "User can connect an existing folder as a project via dialog and see it appear in the list"
    - "User can clone a GitHub repo via dialog and watch the stream output"
    - "User can bootstrap a new project via dialog and watch the stream output"
    - "User can select a project in ExecuteForm and commands dispatch with the correct work_dir from DB"
    - "ExecuteForm falls back to project name as work_dir for node-reported projects not yet in DB"
  artifacts:
    - path: "frontend/src/types/api.ts"
      provides: "ProjectResponse interface"
      contains: "interface ProjectResponse"
    - path: "frontend/src/components/projects/ProjectManager.tsx"
      provides: "Panel listing projects with action buttons"
      contains: "export function ProjectManager"
    - path: "frontend/src/components/projects/ConnectProjectDialog.tsx"
      provides: "Dialog form for connecting existing folder"
      contains: "export function ConnectProjectDialog"
    - path: "frontend/src/components/projects/CloneProjectDialog.tsx"
      provides: "Dialog form for cloning a repo"
      contains: "export function CloneProjectDialog"
    - path: "frontend/src/components/projects/BootstrapProjectDialog.tsx"
      provides: "Dialog form for bootstrapping new project"
      contains: "export function BootstrapProjectDialog"
    - path: "frontend/src/components/execute/ExecuteForm.tsx"
      provides: "Updated project select using DB projects with work_dir resolution"
      contains: "projectMap"
    - path: "frontend/src/routes/dashboard/$nodeId.tsx"
      provides: "ProjectManager panel integrated in left column"
      contains: "ProjectManager"
  key_links:
    - from: "frontend/src/components/projects/ProjectManager.tsx"
      to: "/api/nodes/{node_id}/projects"
      via: "useQuery with queryKey ['projects', nodeId]"
      pattern: "useQuery.*projects.*nodeId"
    - from: "frontend/src/components/projects/ConnectProjectDialog.tsx"
      to: "/api/projects/connect"
      via: "useMutation POST"
      pattern: "api.*projects/connect"
    - from: "frontend/src/components/execute/ExecuteForm.tsx"
      to: "/api/nodes/{node_id}/projects"
      via: "useQuery for projectMap resolution"
      pattern: "projectMap.*work_dir"
    - from: "frontend/src/routes/dashboard/$nodeId.tsx"
      to: "frontend/src/components/projects/ProjectManager.tsx"
      via: "import and render in left panel"
      pattern: "import.*ProjectManager"
---

<objective>
Build the frontend project management UI: ProjectManager panel with project list and action dialogs (connect, clone, bootstrap), update ExecuteForm to resolve work_dir from the projects DB table, and integrate ProjectManager into the node detail page.

Purpose: Enables users to register and manage projects on nodes via a visual interface, and ensures command dispatch uses the correct working directory.
Output: Six new/modified frontend files providing complete project management UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-project-management/14-01-SUMMARY.md

@frontend/src/types/api.ts
@frontend/src/components/execute/ExecuteForm.tsx
@frontend/src/routes/dashboard/$nodeId.tsx
@frontend/src/lib/api.ts
@frontend/src/lib/icons.ts

<interfaces>
<!-- Key types and contracts from Plan 01 backend endpoints -->

Backend API endpoints (created in Plan 01):
```
GET  /api/nodes/{node_id}/projects -> ProjectResponse[]
POST /api/projects/connect  (body: {node_id, name, work_dir})          -> {instance_id: null, project: ProjectResponse}
POST /api/projects/clone    (body: {node_id, name, work_dir, repo_url}) -> {instance_id: string, project: ProjectResponse}
POST /api/projects/bootstrap (body: {node_id, name, work_dir})          -> {instance_id: string, project: ProjectResponse}
```

ProjectResponse shape:
```typescript
interface ProjectResponse {
  id: number
  node_id: string
  name: string
  work_dir: string
  created_at: string
}
```

From frontend/src/types/api.ts (existing):
```typescript
export interface NodeResponse {
  node_id: string; platform: string; version: string;
  projects: string[] | null; status: 'connected' | 'stale' | 'disconnected';
  ...
}
```

From frontend/src/lib/api.ts:
```typescript
export async function api<T>(path: string, options?: RequestInit): Promise<T>
```

From frontend/src/lib/icons.ts (use only imports from this file):
```typescript
// Available icons — check file for full list. Expected to have:
// FolderOpen, GitBranch, Plus, FolderPlus, Link, Loader2, etc.
```

UI components available in frontend/src/components/ui/:
dialog.tsx, input.tsx, button.tsx, select.tsx, badge.tsx, skeleton.tsx
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types, ProjectManager panel, and three dialog components</name>
  <files>
    frontend/src/types/api.ts,
    frontend/src/components/projects/ProjectManager.tsx,
    frontend/src/components/projects/ConnectProjectDialog.tsx,
    frontend/src/components/projects/CloneProjectDialog.tsx,
    frontend/src/components/projects/BootstrapProjectDialog.tsx
  </files>
  <read_first>
    frontend/src/types/api.ts,
    frontend/src/lib/api.ts,
    frontend/src/lib/icons.ts,
    frontend/src/components/execute/ExecuteForm.tsx,
    frontend/src/routes/dashboard/$nodeId.tsx,
    frontend/src/components/ui/dialog.tsx,
    frontend/src/components/ui/input.tsx,
    frontend/src/components/ui/button.tsx
  </read_first>
  <action>
1. Add to `frontend/src/types/api.ts`:
   ```typescript
   export interface ProjectResponse {
     id: number
     node_id: string
     name: string
     work_dir: string
     created_at: string
   }

   export interface ProjectActionResponse {
     instance_id: string | null
     project: ProjectResponse
   }
   ```

2. Create `frontend/src/components/projects/ConnectProjectDialog.tsx`:
   - Controlled `Dialog` with `open`/`setOpen` state
   - Form fields: `name` (Input, required), `work_dir` (Input, required, placeholder: `/home/user/my-project`)
   - Uses `useMutation` calling `api<ProjectActionResponse>('/api/projects/connect', { method: 'POST', body: JSON.stringify({ node_id: nodeId, name, work_dir }) })`
   - On success: calls `onSuccess()` callback (parent will invalidate query + handle instance), closes dialog
   - Trigger button: `<Button variant="outline" size="sm"><Link size={14} className="mr-1" /> Connect</Button>` (import `Link` icon from `@/lib/icons`)
   - Props: `{ nodeId: string; onSuccess: (result: ProjectActionResponse) => void }`
   - Show mutation error below form if present

3. Create `frontend/src/components/projects/CloneProjectDialog.tsx`:
   - Same pattern as ConnectProjectDialog
   - Additional field: `repo_url` (Input, required, placeholder: `https://github.com/user/repo`)
   - Calls `api<ProjectActionResponse>('/api/projects/clone', { method: 'POST', body: JSON.stringify({ node_id: nodeId, name, work_dir, repo_url }) })`
   - Trigger button: `<Button variant="outline" size="sm"><GitBranch size={14} className="mr-1" /> Clone</Button>`
   - Props: `{ nodeId: string; onSuccess: (result: ProjectActionResponse) => void }`

4. Create `frontend/src/components/projects/BootstrapProjectDialog.tsx`:
   - Same pattern as ConnectProjectDialog
   - Only fields: `name` and `work_dir`
   - Calls `api<ProjectActionResponse>('/api/projects/bootstrap', { method: 'POST', body: JSON.stringify({ node_id: nodeId, name, work_dir }) })`
   - Trigger button: `<Button variant="outline" size="sm"><FolderPlus size={14} className="mr-1" /> New</Button>`
   - Props: `{ nodeId: string; onSuccess: (result: ProjectActionResponse) => void }`

5. Create `frontend/src/components/projects/ProjectManager.tsx`:
   - Props: `{ nodeId: string; onInstanceCreated: (instanceId: string) => void }`
   - Uses `useQuery({ queryKey: ['projects', nodeId], queryFn: () => api<ProjectResponse[]>('/api/nodes/${nodeId}/projects') })`
   - Uses `useQueryClient` to get `queryClient`
   - Renders project list: each project shows `name` and `work_dir` in a row
   - Below list, renders three dialog buttons in a `flex gap-2` row
   - Each dialog's `onSuccess` callback:
     a. Calls `queryClient.invalidateQueries({ queryKey: ['projects', nodeId] })`
     b. If `result.instance_id` is not null, calls `onInstanceCreated(result.instance_id)`
   - Container styling matches ExecuteForm pattern: `<div className="space-y-3 p-4 bg-muted/50 rounded-lg border border-border">`
   - Header: `<div className="text-sm font-medium text-muted-foreground uppercase tracking-widest inline-flex items-center gap-1.5"><FolderOpen size={20} className="text-primary" />Projects</div>`
   - Each project row: `<div className="flex items-center justify-between text-sm py-1"><span className="font-mono text-foreground">{p.name}</span><span className="text-muted-foreground text-xs truncate ml-2">{p.work_dir}</span></div>`
   - Empty state when no projects: `<p className="text-sm text-muted-foreground">No projects registered yet.</p>`
   - Import icons from `@/lib/icons` only (FolderOpen, and verify available icons before using Link, GitBranch, FolderPlus — if not available use appropriate alternatives from the icons file)
  </action>
  <verify>
    <automated>cd /Users/josh/code/glsd-server/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/types/api.ts contains `export interface ProjectResponse`
    - frontend/src/types/api.ts contains `export interface ProjectActionResponse`
    - frontend/src/components/projects/ProjectManager.tsx contains `export function ProjectManager`
    - frontend/src/components/projects/ProjectManager.tsx contains `useQuery`
    - frontend/src/components/projects/ProjectManager.tsx contains `queryKey: ['projects'`
    - frontend/src/components/projects/ProjectManager.tsx contains `FolderOpen`
    - frontend/src/components/projects/ConnectProjectDialog.tsx contains `export function ConnectProjectDialog`
    - frontend/src/components/projects/ConnectProjectDialog.tsx contains `/api/projects/connect`
    - frontend/src/components/projects/ConnectProjectDialog.tsx contains `useMutation`
    - frontend/src/components/projects/CloneProjectDialog.tsx contains `export function CloneProjectDialog`
    - frontend/src/components/projects/CloneProjectDialog.tsx contains `/api/projects/clone`
    - frontend/src/components/projects/CloneProjectDialog.tsx contains `repo_url`
    - frontend/src/components/projects/BootstrapProjectDialog.tsx contains `export function BootstrapProjectDialog`
    - frontend/src/components/projects/BootstrapProjectDialog.tsx contains `/api/projects/bootstrap`
    - TypeScript compilation passes with no errors
  </acceptance_criteria>
  <done>ProjectResponse and ProjectActionResponse types added. ProjectManager component renders project list from API with three action dialog buttons. Each dialog posts to its respective endpoint, invalidates the projects query on success, and triggers onInstanceCreated for clone/bootstrap.</done>
</task>

<task type="auto">
  <name>Task 2: Update ExecuteForm work_dir resolution and integrate ProjectManager into node detail page</name>
  <files>
    frontend/src/components/execute/ExecuteForm.tsx,
    frontend/src/routes/dashboard/$nodeId.tsx
  </files>
  <read_first>
    frontend/src/components/execute/ExecuteForm.tsx,
    frontend/src/routes/dashboard/$nodeId.tsx,
    frontend/src/types/api.ts,
    frontend/src/components/projects/ProjectManager.tsx
  </read_first>
  <action>
1. Modify `frontend/src/components/execute/ExecuteForm.tsx`:
   - Add import: `import { useQuery } from '@tanstack/react-query'` (already imported `useMutation` and `useQueryClient` — add `useQuery` to the existing import)
   - Add import: `import type { ProjectResponse } from '@/types/api'`
   - Inside the component, after existing state declarations, add a projects query:
     ```typescript
     const projectsQuery = useQuery({
       queryKey: ['projects', node.node_id],
       queryFn: () => api<ProjectResponse[]>(`/api/nodes/${node.node_id}/projects`),
     })
     ```
   - Build a project map for work_dir resolution:
     ```typescript
     const projectMap = Object.fromEntries(
       (projectsQuery.data ?? []).map((p) => [p.name, p.work_dir])
     )
     ```
   - Merge the project select options: combine node-reported projects with DB-registered projects (DB may have projects not yet in node.projects):
     ```typescript
     const dbProjectNames = (projectsQuery.data ?? []).map((p) => p.name)
     const allProjects = [...new Set([...projects, ...dbProjectNames])]
     ```
   - Update the projects list rendering to use `allProjects` instead of `projects` in the Select component's map
   - In the `executeMutation.mutationFn`, change `work_dir: project` to `work_dir: projectMap[project] ?? project` — this resolves the correct work_dir from the DB, falling back to the project name for node-reported projects without a DB row

2. Modify `frontend/src/routes/dashboard/$nodeId.tsx`:
   - Add import: `import { ProjectManager } from '@/components/projects/ProjectManager'`
   - In the left panel `<div>` (inside `<ResizablePanel defaultSize={40}>`), insert `<ProjectManager>` between the `<ExecuteForm>` and the instances section:
     ```tsx
     <ExecuteForm
       node={node}
       onInstanceCreated={handleInstanceCreated}
       defaultSessionId={resumeSessionId}
     />

     <ProjectManager
       nodeId={nodeId}
       onInstanceCreated={handleInstanceCreated}
     />

     <div>
       <h3 ...>Instances</h3>
       <InstanceList ... />
     </div>
     ```
  </action>
  <verify>
    <automated>cd /Users/josh/code/glsd-server/frontend && npx tsc --noEmit 2>&1 | head -30 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/execute/ExecuteForm.tsx contains `projectsQuery`
    - frontend/src/components/execute/ExecuteForm.tsx contains `projectMap`
    - frontend/src/components/execute/ExecuteForm.tsx contains `projectMap[project] ?? project`
    - frontend/src/components/execute/ExecuteForm.tsx contains `allProjects`
    - frontend/src/components/execute/ExecuteForm.tsx does NOT contain `work_dir: project,` as a standalone assignment (the old pattern)
    - frontend/src/routes/dashboard/$nodeId.tsx contains `import { ProjectManager }`
    - frontend/src/routes/dashboard/$nodeId.tsx contains `<ProjectManager`
    - frontend/src/routes/dashboard/$nodeId.tsx contains `onInstanceCreated={handleInstanceCreated}`
    - TypeScript compilation passes with no errors
    - Frontend build succeeds
  </acceptance_criteria>
  <done>ExecuteForm resolves work_dir from the projects DB table via projectMap, with fallback to project name for backward compatibility. ProjectManager panel is rendered in the node detail page left column between ExecuteForm and InstanceList.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify project management UI end-to-end</name>
  <files>none — verification only</files>
  <action>
Human verification checkpoint. No automated work in this task — user tests the full project management flow in browser.
Steps for the verifier:
1. Run the Alembic migration: `cd backend && alembic upgrade head` (should apply 0006_add_projects_table)
2. Start the backend and frontend
3. Navigate to a node detail page
4. Verify the ProjectManager panel appears between ExecuteForm and Instances in the left column
5. Click "Connect" — fill in a name and work_dir path — submit. Verify it appears in the project list immediately (no stream output expected).
6. Click "Clone" — fill in a name, work_dir, and a valid GitHub repo URL — submit. Verify an instance stream starts showing git clone output.
7. Click "New" — fill in a name and work_dir — submit. Verify an instance stream starts showing /gsd:new-project output.
8. In ExecuteForm, select a DB-registered project and run a command. Verify the command dispatches with the correct work_dir (not just the project name).
9. Try connecting the same project name again — verify it updates (no error from duplicate).
  </action>
  <verify>
    <automated>cd /Users/josh/code/glsd-server/frontend && npx tsc --noEmit && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>User has visually confirmed: ProjectManager panel renders, all three dialogs work, ExecuteForm dispatches with correct work_dir, and duplicate connect is handled gracefully.</done>
</task>

</tasks>

<verification>
- All frontend files compile with no TypeScript errors
- Frontend build succeeds
- ProjectManager panel visible on node detail page
- All three dialogs (connect, clone, bootstrap) POST to correct endpoints
- ExecuteForm resolves work_dir from projectMap
- Backward compatibility: node-reported projects without DB rows still work (fallback to name)
</verification>

<success_criteria>
- TypeScript compilation: `npx tsc --noEmit` exits 0
- Frontend build: `npm run build` exits 0
- ProjectManager panel renders in left column of node detail page
- Connect creates project row without dispatching execute
- Clone and bootstrap create project row AND dispatch execute
- ExecuteForm uses `projectMap[project] ?? project` for work_dir
</success_criteria>

<output>
After completion, create `.planning/phases/14-project-management/14-02-SUMMARY.md`
</output>
