---
phase: 08-color-system-and-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/index.html
  - frontend/src/index.css
  - frontend/src/main.tsx
  - frontend/src/main.css
autonomous: true
requirements: [CLR-01, CLR-02, CLR-03, CLR-04, CLR-05, CLR-06, TYP-03]

must_haves:
  truths:
    - "The .dark {} CSS block contains the full cyberpunk OKLCH palette"
    - "Glow utility classes glow-cyan and glow-magenta exist in @layer utilities"
    - "Orbitron Variable font is installed and registered as --font-heading"
    - "System monospace stack is registered as --font-mono"
    - "The app renders with dark mode active (class=dark on html)"
    - "main.css is deleted and main.tsx imports index.css"
  artifacts:
    - path: "frontend/src/index.css"
      provides: "All CSS tokens, glow utilities, font imports"
      contains: "oklch(0.10 0.01 250)"
    - path: "frontend/index.html"
      provides: "Dark mode activation"
      contains: 'class="dark"'
    - path: "frontend/src/main.tsx"
      provides: "Correct CSS import"
      contains: "./index.css"
  key_links:
    - from: "frontend/index.html"
      to: "frontend/src/index.css"
      via: "class=dark activates .dark {} block"
      pattern: 'class="dark"'
    - from: "frontend/src/main.tsx"
      to: "frontend/src/index.css"
      via: "CSS import"
      pattern: "./index.css"
    - from: "frontend/src/index.css @theme inline"
      to: "frontend/src/index.css .dark {}"
      via: "var() references"
      pattern: "var\\(--"
---

<objective>
Establish the cyberpunk color palette, glow utilities, and typography tokens as CSS custom properties.

Purpose: This is the foundation layer that all Phase 8 gray-sweep work and all Phase 9/10 component work depends on. Without these tokens active, semantic classes like `bg-background` render the wrong colors.

Output: A fully-configured `index.css` with cyberpunk OKLCH palette in `.dark {}`, glow utility classes, Orbitron font import, mono font token, and the structural fixes (dark class on html, main.css deletion, import fix) that activate everything.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-color-system-and-foundation/08-CONTEXT.md
@.planning/phases/08-color-system-and-foundation/08-RESEARCH.md
@.planning/phases/08-color-system-and-foundation/08-UI-SPEC.md

@frontend/src/index.css
@frontend/src/main.tsx
@frontend/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Orbitron font, fix CSS import chain, activate dark mode</name>
  <files>frontend/package.json, frontend/src/main.tsx, frontend/src/main.css, frontend/index.html</files>
  <read_first>frontend/src/main.tsx, frontend/index.html, frontend/package.json</read_first>
  <action>
1. Install the Orbitron variable font:
   ```bash
   cd frontend && npm install @fontsource-variable/orbitron
   ```

2. Edit `frontend/src/main.tsx` — change line 5 from:
   ```ts
   import './main.css'
   ```
   to:
   ```ts
   import './index.css'
   ```

3. Delete `frontend/src/main.css` (it is an identical duplicate of index.css; confirmed in RESEARCH.md).

4. Edit `frontend/index.html` — change line 2 from:
   ```html
   <html lang="en">
   ```
   to:
   ```html
   <html lang="en" class="dark">
   ```
   This activates all `.dark { }` CSS variable overrides. Without it, the app renders with `:root` (light/white) values.
  </action>
  <verify>
    <automated>cd /Users/josh/code/glsd-server && grep -q 'index.css' frontend/src/main.tsx && grep -q 'class="dark"' frontend/index.html && test ! -f frontend/src/main.css && grep -q '@fontsource-variable/orbitron' frontend/package.json && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/main.tsx` contains `import './index.css'` (not `main.css`)
    - `frontend/src/main.css` does NOT exist
    - `frontend/index.html` contains `<html lang="en" class="dark">`
    - `frontend/package.json` contains `@fontsource-variable/orbitron` in dependencies
  </acceptance_criteria>
  <done>Orbitron installed, main.tsx imports index.css, main.css deleted, dark class on html element</done>
</task>

<task type="auto">
  <name>Task 2: Replace index.css with cyberpunk OKLCH palette, font tokens, and glow utilities</name>
  <files>frontend/src/index.css</files>
  <read_first>frontend/src/index.css, .planning/phases/08-color-system-and-foundation/08-UI-SPEC.md, .planning/phases/08-color-system-and-foundation/08-RESEARCH.md</read_first>
  <action>
Edit `frontend/src/index.css` to contain the following complete content. This replaces the entire file.

**Import section** (lines 1-5) — add Orbitron import after Geist:
```css
@import "tailwindcss";
@import "tw-animate-css";
@import "shadcn/tailwind.css";
@import "@fontsource-variable/geist";
@import "@fontsource-variable/orbitron";
```

**Custom variant** (unchanged):
```css
@custom-variant dark (&:is(.dark *));
```

**@theme inline block** — REPLACE `--font-heading: var(--font-sans)` with Orbitron. ADD `--font-mono`. Keep all existing `--color-*` and `--radius-*` entries unchanged:
```css
@theme inline {
    --font-heading: 'Orbitron Variable', sans-serif;
    --font-sans: 'Geist Variable', sans-serif;
    --font-mono: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas,
                 'Liberation Mono', 'Courier New', monospace;
    --color-sidebar-ring: var(--sidebar-ring);
    --color-sidebar-border: var(--sidebar-border);
    --color-sidebar-accent-foreground: var(--sidebar-accent-foreground);
    --color-sidebar-accent: var(--sidebar-accent);
    --color-sidebar-primary-foreground: var(--sidebar-primary-foreground);
    --color-sidebar-primary: var(--sidebar-primary);
    --color-sidebar-foreground: var(--sidebar-foreground);
    --color-sidebar: var(--sidebar);
    --color-chart-5: var(--chart-5);
    --color-chart-4: var(--chart-4);
    --color-chart-3: var(--chart-3);
    --color-chart-2: var(--chart-2);
    --color-chart-1: var(--chart-1);
    --color-ring: var(--ring);
    --color-input: var(--input);
    --color-border: var(--border);
    --color-destructive: var(--destructive);
    --color-accent-foreground: var(--accent-foreground);
    --color-accent: var(--accent);
    --color-muted-foreground: var(--muted-foreground);
    --color-muted: var(--muted);
    --color-secondary-foreground: var(--secondary-foreground);
    --color-secondary: var(--secondary);
    --color-primary-foreground: var(--primary-foreground);
    --color-primary: var(--primary);
    --color-popover-foreground: var(--popover-foreground);
    --color-popover: var(--popover);
    --color-card-foreground: var(--card-foreground);
    --color-card: var(--card);
    --color-foreground: var(--foreground);
    --color-background: var(--background);
    --radius-sm: calc(var(--radius) * 0.6);
    --radius-md: calc(var(--radius) * 0.8);
    --radius-lg: var(--radius);
    --radius-xl: calc(var(--radius) * 1.4);
    --radius-2xl: calc(var(--radius) * 1.8);
    --radius-3xl: calc(var(--radius) * 2.2);
    --radius-4xl: calc(var(--radius) * 2.6);
}
```

**:root block** — keep existing light-mode values unchanged (required by shadcn components that check :root).

**.dark block** — REPLACE all values with the cyberpunk OKLCH palette. The complete `.dark { }` block:
```css
.dark {
    --background:  oklch(0.10 0.01 250);
    --foreground:  oklch(0.92 0.02 195);
    --card:        oklch(0.14 0.01 250);
    --card-foreground: oklch(0.92 0.02 195);
    --popover:     oklch(0.14 0.01 250);
    --popover-foreground: oklch(0.92 0.02 195);
    --primary:          oklch(0.75 0.18 195);
    --primary-foreground: oklch(0.10 0.01 250);
    --secondary:          oklch(0.16 0.01 250);
    --secondary-foreground: oklch(0.85 0.02 195);
    --muted:          oklch(0.18 0.01 250);
    --muted-foreground: oklch(0.60 0.04 195);
    --accent:          oklch(0.70 0.25 330);
    --accent-foreground: oklch(0.10 0.01 250);
    --destructive: oklch(0.65 0.22 25);
    --border: oklch(0.75 0.18 195 / 15%);
    --input:  oklch(0.75 0.18 195 / 10%);
    --ring: oklch(0.75 0.18 195);
    --chart-1: oklch(0.75 0.18 195);
    --chart-2: oklch(0.70 0.25 330);
    --chart-3: oklch(0.75 0.18 270);
    --chart-4: oklch(0.80 0.15 145);
    --chart-5: oklch(0.75 0.20 55);
    --radius: 0.625rem;
    --sidebar:          oklch(0.12 0.01 250);
    --sidebar-foreground: oklch(0.92 0.02 195);
    --sidebar-primary:  oklch(0.75 0.18 195);
    --sidebar-primary-foreground: oklch(0.10 0.01 250);
    --sidebar-accent:   oklch(0.16 0.01 250);
    --sidebar-accent-foreground: oklch(0.92 0.02 195);
    --sidebar-border:   oklch(0.75 0.18 195 / 15%);
    --sidebar-ring:     oklch(0.75 0.18 195);
}
```

**@layer base** — keep unchanged:
```css
@layer base {
  * {
    @apply border-border outline-ring/50;
  }
  body {
    @apply bg-background text-foreground;
  }
  html {
    @apply font-sans;
  }
}
```

**ADD after @layer base — glow utility classes** in `@layer utilities`:
```css
@layer utilities {
    .glow-cyan {
        position: relative;
        isolation: isolate;
    }
    .glow-cyan::after {
        content: '';
        position: absolute;
        inset: -2px;
        border-radius: inherit;
        background: transparent;
        box-shadow: 0 0 8px 2px oklch(0.75 0.18 195 / 60%);
        opacity: 0;
        transition: opacity 200ms ease-out;
        pointer-events: none;
        z-index: -1;
    }
    .glow-cyan:hover::after,
    .glow-cyan:focus-visible::after {
        opacity: 1;
    }

    .glow-magenta {
        position: relative;
        isolation: isolate;
    }
    .glow-magenta::after {
        content: '';
        position: absolute;
        inset: -2px;
        border-radius: inherit;
        background: transparent;
        box-shadow: 0 0 8px 2px oklch(0.70 0.25 330 / 60%);
        opacity: 0;
        transition: opacity 200ms ease-out;
        pointer-events: none;
        z-index: -1;
    }
    .glow-magenta:hover::after,
    .glow-magenta:focus-visible::after {
        opacity: 1;
    }
}
```

CRITICAL RULES:
- Do NOT put raw OKLCH values inside `@theme inline` — only `var(--token)` references
- Do NOT change `:root` values — shadcn components expect them
- The `--font-heading` line in `@theme inline` must be REPLACED (from `var(--font-sans)` to `'Orbitron Variable', sans-serif`), not duplicated
  </action>
  <verify>
    <automated>cd /Users/josh/code/glsd-server && grep -q "oklch(0.10 0.01 250)" frontend/src/index.css && grep -q "oklch(0.75 0.18 195)" frontend/src/index.css && grep -q "oklch(0.70 0.25 330)" frontend/src/index.css && grep -q "glow-cyan" frontend/src/index.css && grep -q "glow-magenta" frontend/src/index.css && grep -q "Orbitron Variable" frontend/src/index.css && grep -q "font-mono" frontend/src/index.css && grep -q "@fontsource-variable/orbitron" frontend/src/index.css && ! grep -q "font-heading: var(--font-sans)" frontend/src/index.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/index.css` contains `--background:  oklch(0.10 0.01 250)` in .dark block (CLR-01)
    - `frontend/src/index.css` contains `--primary:          oklch(0.75 0.18 195)` in .dark block (CLR-02)
    - `frontend/src/index.css` contains `--accent:          oklch(0.70 0.25 330)` in .dark block (CLR-03)
    - `frontend/src/index.css` contains `--border: oklch(0.75 0.18 195 / 15%)` in .dark block (CLR-04)
    - `frontend/src/index.css` contains `.glow-cyan` and `.glow-magenta` class definitions in `@layer utilities` (CLR-05)
    - `frontend/src/index.css` contains `--ring: oklch(0.75 0.18 195)` in .dark block (CLR-06)
    - `frontend/src/index.css` contains `@import "@fontsource-variable/orbitron"` (TYP-03)
    - `frontend/src/index.css` contains `--font-heading: 'Orbitron Variable', sans-serif` in @theme inline (TYP-03)
    - `frontend/src/index.css` contains `--font-mono: ui-monospace` in @theme inline (TYP-02 token)
    - `frontend/src/index.css` does NOT contain `--font-heading: var(--font-sans)` (old value replaced)
    - `:root` block values are unchanged from original
    - No raw OKLCH values appear inside `@theme inline { }` block
  </acceptance_criteria>
  <done>index.css has cyberpunk OKLCH palette in .dark, glow utilities in @layer utilities, Orbitron import and --font-heading token, --font-mono token, and all @theme inline entries use var() references only</done>
</task>

</tasks>

<verification>
1. `cd frontend && npm run build` completes without errors (Tailwind processes all tokens)
2. `grep -q "oklch(0.10 0.01 250)" frontend/src/index.css` confirms void-black background
3. `grep -q 'class="dark"' frontend/index.html` confirms dark mode is active
4. `grep -q "glow-cyan" frontend/src/index.css` confirms glow utilities exist
5. `test ! -f frontend/src/main.css` confirms duplicate deleted
</verification>

<success_criteria>
- The CSS foundation is complete: all cyberpunk OKLCH tokens defined in .dark, glow utilities available, Orbitron font installed and registered, mono font registered
- The structural fixes are in place: dark class on html, main.css deleted, main.tsx imports index.css
- `npm run build` succeeds in the frontend directory
</success_criteria>

<output>
After completion, create `.planning/phases/08-color-system-and-foundation/08-01-SUMMARY.md`
</output>
