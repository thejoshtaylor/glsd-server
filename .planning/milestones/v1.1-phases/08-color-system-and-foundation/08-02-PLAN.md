---
phase: 08-color-system-and-foundation
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - frontend/src/routes/__root.tsx
  - frontend/src/routes/dashboard/$nodeId.tsx
  - frontend/src/routes/dashboard/audit.tsx
  - frontend/src/routes/login.tsx
autonomous: true
requirements: [CLR-01, CLR-02, CLR-04, TYP-01, TYP-02]

must_haves:
  truths:
    - "All route files use semantic Tailwind classes (bg-background, text-foreground, etc.) instead of hardcoded gray-* classes"
    - "Section headings in routes render uppercase with letter-spacing"
    - "Data fields (IDs, timestamps) in routes use font-mono"
    - "Page titles in routes use font-heading (Orbitron)"
  artifacts:
    - path: "frontend/src/routes/__root.tsx"
      provides: "Root layout with semantic classes"
      min_lines: 20
    - path: "frontend/src/routes/dashboard/$nodeId.tsx"
      provides: "Node detail view with semantic classes"
      min_lines: 50
    - path: "frontend/src/routes/login.tsx"
      provides: "Login page with semantic classes"
      min_lines: 30
  key_links:
    - from: "frontend/src/routes/__root.tsx"
      to: "frontend/src/index.css"
      via: "bg-background, text-foreground resolve to .dark CSS vars"
      pattern: "bg-background"
---

<objective>
Sweep all hardcoded `gray-*` Tailwind classes from route files and apply typography tokens to headings and data fields.

Purpose: Routes are the top-level layout containers. After this plan, every route renders with cyberpunk palette colors via semantic CSS variables instead of hardcoded grays. Typography tokens (uppercase headings, monospace data, Orbitron titles) are applied where routes define section labels and data displays.

Output: 4 route files updated — zero `gray-*` classes remaining, typography applied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-color-system-and-foundation/08-CONTEXT.md
@.planning/phases/08-color-system-and-foundation/08-RESEARCH.md
@.planning/phases/08-color-system-and-foundation/08-UI-SPEC.md

@frontend/src/routes/__root.tsx
@frontend/src/routes/dashboard/$nodeId.tsx
@frontend/src/routes/dashboard/audit.tsx
@frontend/src/routes/login.tsx

<interfaces>
<!-- Gray class → semantic class mapping (from RESEARCH.md) -->
| Hardcoded class | Semantic replacement |
|-----------------|---------------------|
| `bg-gray-950` | `bg-background` |
| `bg-gray-900` | `bg-card` or `bg-sidebar` (context-dependent) |
| `bg-gray-900/50`, `bg-gray-800/50` | `bg-card/50` or `bg-muted/50` |
| `bg-gray-800` | `bg-muted` |
| `border-gray-800`, `border-gray-700` | `border-border` |
| `text-gray-100`, `text-gray-200` | `text-foreground` |
| `text-gray-300`, `text-gray-400` | `text-muted-foreground` |
| `text-gray-500`, `text-gray-600` | `text-muted-foreground` |
| `hover:bg-gray-800`, `hover:bg-gray-700/50` | `hover:bg-muted` |
| `text-white` | `text-foreground` |

<!-- Typography tokens (from CONTEXT.md + UI-SPEC.md) -->
Section headings: add `uppercase tracking-widest` (TYP-01, 0.1em letter-spacing)
Data fields (IDs, timestamps): add `font-mono` (TYP-02)
Page titles: add `font-heading` (TYP-03, Orbitron)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace gray classes in __root.tsx and login.tsx with semantic tokens + typography</name>
  <files>frontend/src/routes/__root.tsx, frontend/src/routes/login.tsx</files>
  <read_first>frontend/src/routes/__root.tsx, frontend/src/routes/login.tsx</read_first>
  <action>
**__root.tsx** — replace all gray-* classes. The file has 7 occurrences. Apply these specific replacements:

Line 25 (`div className="flex h-screen bg-gray-950 text-gray-100"`):
- `bg-gray-950` → `bg-background`
- `text-gray-100` → `text-foreground`

Line 27 (`aside className="w-56 border-r border-gray-800 bg-gray-900 flex flex-col"`):
- `border-gray-800` → `border-border`
- `bg-gray-900` → `bg-sidebar`

Line 28 (`div className="p-4 border-b border-gray-800"`):
- `border-gray-800` → `border-border`

Line 34 and 40 (nav links `text-gray-300 hover:bg-gray-800 hover:text-white [&.active]:bg-gray-800 [&.active]:text-white`):
- `text-gray-300` → `text-muted-foreground`
- `hover:bg-gray-800` → `hover:bg-muted`
- `hover:text-white` → `hover:text-foreground`
- `[&.active]:bg-gray-800` → `[&.active]:bg-muted`
- `[&.active]:text-white` → `[&.active]:text-foreground`

Line 45 (`div className="p-3 border-t border-gray-800"`):
- `border-gray-800` → `border-border`

Line 46 (Button `text-gray-400`):
- `text-gray-400` → `text-muted-foreground`

Also apply TYP-03 (Orbitron) to the app title if there's a heading in the sidebar header (the "p-4 border-b" div). If the sidebar header contains a title/brand text, add `font-heading` to it.

**login.tsx** — replace all gray-* classes. The file has 4 occurrences:

Line 57 (`div className="... bg-gray-950"`):
- `bg-gray-950` → `bg-background`

Line 58 (`Card className="... bg-gray-900 border-gray-800"`):
- `bg-gray-900` → `bg-card`
- `border-gray-800` → `border-border`

Line 70 (`className="bg-gray-800 border-gray-700 text-white"`):
- `bg-gray-800` → `bg-muted`
- `border-gray-700` → `border-border`
- `text-white` → `text-foreground`

Line 78 (`className="bg-gray-800 border-gray-700 text-white"`):
- Same replacements as line 70

If the login page has a title heading, add `font-heading` class to it for TYP-03 (Orbitron on page titles).
  </action>
  <verify>
    <automated>cd /Users/josh/code/glsd-server && ! grep -q "gray-" frontend/src/routes/__root.tsx && ! grep -q "gray-" frontend/src/routes/login.tsx && grep -q "bg-background" frontend/src/routes/__root.tsx && grep -q "bg-card" frontend/src/routes/login.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/routes/__root.tsx` contains zero occurrences of `gray-`
    - `frontend/src/routes/__root.tsx` contains `bg-background`
    - `frontend/src/routes/__root.tsx` contains `bg-sidebar`
    - `frontend/src/routes/__root.tsx` contains `border-border`
    - `frontend/src/routes/__root.tsx` contains `text-muted-foreground`
    - `frontend/src/routes/login.tsx` contains zero occurrences of `gray-`
    - `frontend/src/routes/login.tsx` contains `bg-background`
    - `frontend/src/routes/login.tsx` contains `bg-card`
    - `frontend/src/routes/login.tsx` contains `border-border`
  </acceptance_criteria>
  <done>__root.tsx and login.tsx use only semantic Tailwind classes; zero gray-* occurrences remain</done>
</task>

<task type="auto">
  <name>Task 2: Replace gray classes in $nodeId.tsx and audit.tsx with semantic tokens + typography</name>
  <files>frontend/src/routes/dashboard/$nodeId.tsx, frontend/src/routes/dashboard/audit.tsx</files>
  <read_first>frontend/src/routes/dashboard/$nodeId.tsx, frontend/src/routes/dashboard/audit.tsx</read_first>
  <action>
**$nodeId.tsx** — replace all gray-* classes. The file has 9 occurrences:

Line 75 (`text-gray-400`): → `text-muted-foreground`

Line 83 (Button `text-gray-400`): → `text-muted-foreground`

Line 94 (`text-gray-500` and `text-gray-200`):
- `text-gray-500` → `text-muted-foreground`
- `text-gray-200` → `text-foreground`

Line 95: same pattern as 94

Line 96: same pattern as 94

Line 101 (`border border-gray-700`): → `border border-border`

Line 111 (`text-sm font-medium text-gray-300 mb-2`):
- `text-gray-300` → `text-muted-foreground`
- This is an "Instances" section heading — also add `uppercase tracking-widest` for TYP-01

Line 124 (`bg-gray-900/30`): → `bg-card/30`

Line 136 (`text-gray-500`): → `text-muted-foreground`

Also apply typography:
- TYP-01: The "Instances" h3 on line 111 — add `uppercase tracking-widest` classes
- TYP-02: The Platform/Version/Projects data values on lines 94-96 are metadata labels. The node detail identifiers should use `font-mono` where they display IDs or raw data values.
- TYP-03: If $nodeId.tsx has a page-level title (node name heading), add `font-heading` class

**audit.tsx** — replace all gray-* classes. The file has 2 occurrences:

Line 80 (`text-gray-500`): → `text-muted-foreground`

Line 100 (`text-gray-500`): → `text-muted-foreground`

Also apply typography:
- TYP-01: If audit.tsx has section headings, add `uppercase tracking-widest`
  </action>
  <verify>
    <automated>cd /Users/josh/code/glsd-server && ! grep -q "gray-" frontend/src/routes/dashboard/\$nodeId.tsx && ! grep -q "gray-" frontend/src/routes/dashboard/audit.tsx && grep -q "bg-background\|bg-card\|border-border\|text-foreground\|text-muted-foreground" frontend/src/routes/dashboard/\$nodeId.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/routes/dashboard/$nodeId.tsx` contains zero occurrences of `gray-`
    - `frontend/src/routes/dashboard/$nodeId.tsx` contains `border-border`
    - `frontend/src/routes/dashboard/$nodeId.tsx` contains `text-muted-foreground`
    - `frontend/src/routes/dashboard/$nodeId.tsx` contains `uppercase tracking-widest` on the Instances heading (TYP-01)
    - `frontend/src/routes/dashboard/audit.tsx` contains zero occurrences of `gray-`
    - `frontend/src/routes/dashboard/audit.tsx` contains `text-muted-foreground`
  </acceptance_criteria>
  <done>$nodeId.tsx and audit.tsx use only semantic Tailwind classes with typography tokens applied; zero gray-* occurrences remain</done>
</task>

</tasks>

<verification>
1. `grep -r "gray-" frontend/src/routes/ --include="*.tsx" | wc -l` returns 0
2. `grep -r "bg-background\|bg-card\|bg-sidebar\|border-border\|text-foreground\|text-muted-foreground" frontend/src/routes/ --include="*.tsx" | wc -l` returns > 0
3. `grep -r "uppercase tracking-widest" frontend/src/routes/ --include="*.tsx" | wc -l` returns > 0
4. `cd frontend && npm run build` succeeds
</verification>

<success_criteria>
- Zero `gray-*` Tailwind classes remain in any file under `frontend/src/routes/`
- All replaced classes use semantic tokens (bg-background, text-foreground, border-border, etc.)
- Section headings have uppercase + tracking-widest applied (TYP-01)
- Data fields use font-mono where appropriate (TYP-02)
- Page titles use font-heading where appropriate (TYP-03)
- `npm run build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/08-color-system-and-foundation/08-02-SUMMARY.md`
</output>
