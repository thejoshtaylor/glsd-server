---
phase: 08-color-system-and-foundation
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - frontend/src/components/ui/button.tsx
  - frontend/src/components/stream/StreamPanel.tsx
  - frontend/src/components/stream/HistoryStreamPanel.tsx
  - frontend/src/components/stream/StreamEventRenderer.tsx
  - frontend/src/components/stream/AssistantText.tsx
  - frontend/src/components/stream/SystemEvent.tsx
  - frontend/src/components/stream/ToolUse.tsx
  - frontend/src/components/stream/ToolResult.tsx
  - frontend/src/components/stream/ResultSummary.tsx
  - frontend/src/components/nodes/NodeCard.tsx
  - frontend/src/components/nodes/NodeGrid.tsx
  - frontend/src/components/nodes/InstanceList.tsx
  - frontend/src/components/execute/ExecuteForm.tsx
  - frontend/src/components/audit/AuditTable.tsx
autonomous: true
requirements: [CLR-01, CLR-02, CLR-04, CLR-05, TYP-01, TYP-02]

must_haves:
  truths:
    - "All component files use semantic Tailwind classes instead of hardcoded gray-* classes"
    - "The Button default variant has glow-cyan class applied"
    - "Stream output text renders in monospace font"
    - "Component section headings render uppercase with letter-spacing"
  artifacts:
    - path: "frontend/src/components/ui/button.tsx"
      provides: "Button with glow-cyan on default variant"
      contains: "glow-cyan"
    - path: "frontend/src/components/nodes/NodeCard.tsx"
      provides: "NodeCard with semantic classes"
      min_lines: 10
    - path: "frontend/src/components/stream/StreamPanel.tsx"
      provides: "StreamPanel with semantic classes"
      min_lines: 15
  key_links:
    - from: "frontend/src/components/ui/button.tsx"
      to: "frontend/src/index.css"
      via: "glow-cyan class triggers ::after pseudo-element from @layer utilities"
      pattern: "glow-cyan"
---

<objective>
Sweep all hardcoded `gray-*` Tailwind classes from component files, apply glow-cyan to the Button default variant, and add typography tokens to stream output and component headings.

Purpose: Components are the building blocks used across all routes. After this plan, every component renders with cyberpunk palette colors, the Button has a visible neon glow on hover, stream output uses monospace, and component-level section headings have the cyberpunk uppercase treatment.

Output: 14 component files updated — zero `gray-*` classes remaining, Button has glow, typography applied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-color-system-and-foundation/08-CONTEXT.md
@.planning/phases/08-color-system-and-foundation/08-RESEARCH.md
@.planning/phases/08-color-system-and-foundation/08-UI-SPEC.md

@frontend/src/components/ui/button.tsx
@frontend/src/components/nodes/NodeCard.tsx
@frontend/src/components/nodes/NodeGrid.tsx
@frontend/src/components/nodes/InstanceList.tsx
@frontend/src/components/stream/StreamPanel.tsx
@frontend/src/components/stream/HistoryStreamPanel.tsx
@frontend/src/components/execute/ExecuteForm.tsx
@frontend/src/components/audit/AuditTable.tsx

<interfaces>
<!-- Gray class → semantic class mapping (from RESEARCH.md) -->
| Hardcoded class | Semantic replacement |
|-----------------|---------------------|
| `bg-gray-950` | `bg-background` |
| `bg-gray-900` | `bg-card` or `bg-sidebar` (context-dependent) |
| `bg-gray-900/50`, `bg-gray-800/50` | `bg-card/50` or `bg-muted/50` |
| `bg-gray-800` | `bg-muted` |
| `bg-gray-800/80` | `bg-muted/80` |
| `border-gray-800`, `border-gray-700`, `border-gray-700/50`, `border-gray-600`, `border-gray-600/30` | `border-border` |
| `text-gray-100`, `text-gray-200` | `text-foreground` |
| `text-gray-300`, `text-gray-400` | `text-muted-foreground` |
| `text-gray-500`, `text-gray-600` | `text-muted-foreground` |
| `hover:bg-gray-800`, `hover:bg-gray-700/50` | `hover:bg-muted` |
| `text-white` | `text-foreground` |
| `bg-gray-600/20` | `bg-muted/20` |
| `focus:ring-blue-500` | `focus:ring-ring` (uses neon cyan via --ring token) |

<!-- Button glow application (from UI-SPEC.md) -->
Add `glow-cyan` to the default variant string in button.tsx buttonVariants.

<!-- Typography (from CONTEXT.md) -->
Section headings (h3 labels): add `uppercase tracking-widest` (TYP-01)
Stream output / IDs / timestamps: ensure `font-mono` is present (TYP-02)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add glow-cyan to Button default variant and sweep node/execute/audit components</name>
  <files>frontend/src/components/ui/button.tsx, frontend/src/components/nodes/NodeCard.tsx, frontend/src/components/nodes/NodeGrid.tsx, frontend/src/components/nodes/InstanceList.tsx, frontend/src/components/execute/ExecuteForm.tsx, frontend/src/components/audit/AuditTable.tsx</files>
  <read_first>frontend/src/components/ui/button.tsx, frontend/src/components/nodes/NodeCard.tsx, frontend/src/components/nodes/NodeGrid.tsx, frontend/src/components/nodes/InstanceList.tsx, frontend/src/components/execute/ExecuteForm.tsx, frontend/src/components/audit/AuditTable.tsx</read_first>
  <action>
**button.tsx** — Add `glow-cyan` to the default variant. Edit line 11 from:
```
default: "bg-primary text-primary-foreground [a]:hover:bg-primary/80",
```
to:
```
default: "bg-primary text-primary-foreground [a]:hover:bg-primary/80 glow-cyan",
```
This satisfies CLR-05 — default buttons get visible neon glow on hover/focus.

**NodeCard.tsx** — 4 occurrences:

Line 15 (`bg-gray-900 border-gray-800 hover:border-gray-700`):
- `bg-gray-900` → `bg-card`
- `border-gray-800` → `border-border`
- `hover:border-gray-700` → `hover:border-primary/30`

Line 23, 27 (`text-gray-400`): → `text-muted-foreground`

Line 30 (`text-gray-500`): → `text-muted-foreground`

**NodeGrid.tsx** — 2 occurrences:

Line 12 (`text-gray-400`): → `text-muted-foreground`

Line 14 (`text-gray-400`): → `text-muted-foreground`

**InstanceList.tsx** — 7 occurrences:

Line 10 (`bg-gray-600/20 text-gray-400 border-gray-600/30`):
- `bg-gray-600/20` → `bg-muted/20`
- `text-gray-400` → `text-muted-foreground`
- `border-gray-600/30` → `border-border`

Line 26 (`text-gray-400`): → `text-muted-foreground`

Line 27 (`text-gray-500`): → `text-muted-foreground`

Line 34 (`bg-gray-800/50 ... border-gray-700/50 ... hover:bg-gray-700/50`):
- `bg-gray-800/50` → `bg-muted/50`
- `border-gray-700/50` → `border-border`
- `hover:bg-gray-700/50` → `hover:bg-muted`

Line 39 (`text-gray-200`): → `text-foreground`

Line 40 (`text-gray-500`): → `text-muted-foreground`

Line 43 (`text-gray-400`): → `text-muted-foreground`

Also for TYP-02: Instance IDs displayed in InstanceList should have `font-mono` class if not already present.

**ExecuteForm.tsx** — 7 occurrences:

Line 88 (`bg-gray-800/50 ... border-gray-700/50`):
- `bg-gray-800/50` → `bg-muted/50`
- `border-gray-700/50` → `border-border`

Line 89 (`text-gray-300`): → `text-muted-foreground`
- This is a section heading "Execute Command" — also add `uppercase tracking-widest` for TYP-01

Line 95 (`bg-gray-800 border border-gray-700 text-gray-200`):
- `bg-gray-800` → `bg-muted`
- `border-gray-700` → `border-border`
- `text-gray-200` → `text-foreground`

Line 111 (`bg-gray-800 border border-gray-700 text-gray-200 ... focus:ring-blue-500`):
- `bg-gray-800` → `bg-muted`
- `border-gray-700` → `border-border`
- `text-gray-200` → `text-foreground`
- `focus:ring-blue-500` → `focus:ring-ring`

Line 114 (`bg-gray-800/80`): → `bg-muted/80`

Line 115 (`text-gray-400`): → `text-muted-foreground`

Line 130 (`bg-gray-800 border-gray-700 text-gray-200`):
- `bg-gray-800` → `bg-muted`
- `border-gray-700` → `border-border`
- `text-gray-200` → `text-foreground`

**AuditTable.tsx** — 3 occurrences:

Line 67 (`text-gray-500`): → `text-muted-foreground`

Line 72 (`hover:bg-gray-800`): → `hover:bg-muted`

Line 93 (`text-gray-500`): → `text-muted-foreground`
  </action>
  <verify>
    <automated>cd /Users/josh/code/glsd-server && ! grep -q "gray-" frontend/src/components/ui/button.tsx && ! grep -q "gray-" frontend/src/components/nodes/NodeCard.tsx && ! grep -q "gray-" frontend/src/components/nodes/NodeGrid.tsx && ! grep -q "gray-" frontend/src/components/nodes/InstanceList.tsx && ! grep -q "gray-" frontend/src/components/execute/ExecuteForm.tsx && ! grep -q "gray-" frontend/src/components/audit/AuditTable.tsx && grep -q "glow-cyan" frontend/src/components/ui/button.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/ui/button.tsx` default variant contains `glow-cyan` (CLR-05)
    - `frontend/src/components/ui/button.tsx` contains zero occurrences of `gray-`
    - `frontend/src/components/nodes/NodeCard.tsx` contains zero occurrences of `gray-`
    - `frontend/src/components/nodes/NodeCard.tsx` contains `bg-card` and `border-border`
    - `frontend/src/components/nodes/NodeGrid.tsx` contains zero occurrences of `gray-`
    - `frontend/src/components/nodes/InstanceList.tsx` contains zero occurrences of `gray-`
    - `frontend/src/components/execute/ExecuteForm.tsx` contains zero occurrences of `gray-`
    - `frontend/src/components/execute/ExecuteForm.tsx` contains `uppercase tracking-widest` on Execute Command heading (TYP-01)
    - `frontend/src/components/execute/ExecuteForm.tsx` does not contain `focus:ring-blue-500` (replaced with `focus:ring-ring`)
    - `frontend/src/components/audit/AuditTable.tsx` contains zero occurrences of `gray-`
  </acceptance_criteria>
  <done>Button has glow-cyan, all node/execute/audit components use semantic classes, zero gray-* remaining in these files</done>
</task>

<task type="auto">
  <name>Task 2: Sweep gray classes from all stream components and apply monospace typography</name>
  <files>frontend/src/components/stream/StreamPanel.tsx, frontend/src/components/stream/HistoryStreamPanel.tsx, frontend/src/components/stream/StreamEventRenderer.tsx, frontend/src/components/stream/AssistantText.tsx, frontend/src/components/stream/SystemEvent.tsx, frontend/src/components/stream/ToolUse.tsx, frontend/src/components/stream/ToolResult.tsx, frontend/src/components/stream/ResultSummary.tsx</files>
  <read_first>frontend/src/components/stream/StreamPanel.tsx, frontend/src/components/stream/HistoryStreamPanel.tsx, frontend/src/components/stream/StreamEventRenderer.tsx, frontend/src/components/stream/AssistantText.tsx, frontend/src/components/stream/SystemEvent.tsx, frontend/src/components/stream/ToolUse.tsx, frontend/src/components/stream/ToolResult.tsx, frontend/src/components/stream/ResultSummary.tsx</read_first>
  <action>
**StreamPanel.tsx** — 4 occurrences:

Line 22 (`border-gray-700 bg-gray-900/50`):
- `border-gray-700` → `border-border`
- `bg-gray-900/50` → `bg-card/50`

Line 23 (`text-gray-300`): → `text-muted-foreground`
- This is a "Stream:" section label — add `uppercase tracking-widest` for TYP-01

Line 24 (`text-gray-400 font-mono`): → `text-muted-foreground font-mono`
- Already has `font-mono` for the instance ID — good for TYP-02

Line 35 (`text-gray-500`): → `text-muted-foreground`

**HistoryStreamPanel.tsx** — 5 occurrences:

Line 17 (`text-gray-400`): → `text-muted-foreground`

Line 19 (`text-gray-500`): → `text-muted-foreground`

Line 23 (`border-gray-700 bg-gray-900/50`):
- `border-gray-700` → `border-border`
- `bg-gray-900/50` → `bg-card/50`

Line 24 (`text-gray-300`): → `text-muted-foreground`
- This is a "History:" section label — add `uppercase tracking-widest` for TYP-01

Line 25 (`text-gray-400 font-mono`): → `text-muted-foreground font-mono`
- Already has `font-mono` — TYP-02 satisfied

**StreamEventRenderer.tsx** — 1 occurrence:

Line 21 (`text-gray-600`): → `text-muted-foreground`

**AssistantText.tsx** — 1 occurrence:

Line 11 (`text-gray-200 ... font-mono`):
- `text-gray-200` → `text-foreground`
- Already has `font-mono` — TYP-02 satisfied

**SystemEvent.tsx** — 2 occurrences:

Line 5 (`border-gray-600`): → `border-border`

Line 6 (`text-gray-500 font-mono`): → `text-muted-foreground font-mono`
- Already has `font-mono` — TYP-02 satisfied

**ToolUse.tsx** — 1 occurrence:

Line 19 (`text-gray-400 font-mono`): → `text-muted-foreground font-mono`
- Already has `font-mono` — TYP-02 satisfied

**ToolResult.tsx** — 1 occurrence:

Line 12 (`text-gray-300 ... font-mono`): → `text-foreground ... font-mono`
- Already has `font-mono` — TYP-02 satisfied

**ResultSummary.tsx** — 2 occurrences:

Line 10 (`text-gray-300 ... font-mono`): → `text-foreground ... font-mono`
- Already has `font-mono` — TYP-02 satisfied

Line 13 (`text-gray-500`): → `text-muted-foreground`

TYP-02 note: All stream components already use `font-mono` on code/data output elements. The `--font-mono` token from Plan 01 wires this to the system monospace stack. No additional `font-mono` class additions are needed — just verify they remain in place after the gray sweep.
  </action>
  <verify>
    <automated>cd /Users/josh/code/glsd-server && ! grep -q "gray-" frontend/src/components/stream/StreamPanel.tsx && ! grep -q "gray-" frontend/src/components/stream/HistoryStreamPanel.tsx && ! grep -q "gray-" frontend/src/components/stream/StreamEventRenderer.tsx && ! grep -q "gray-" frontend/src/components/stream/AssistantText.tsx && ! grep -q "gray-" frontend/src/components/stream/SystemEvent.tsx && ! grep -q "gray-" frontend/src/components/stream/ToolUse.tsx && ! grep -q "gray-" frontend/src/components/stream/ToolResult.tsx && ! grep -q "gray-" frontend/src/components/stream/ResultSummary.tsx && grep -q "font-mono" frontend/src/components/stream/StreamPanel.tsx && grep -q "uppercase tracking-widest" frontend/src/components/stream/StreamPanel.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - All 8 stream component files contain zero occurrences of `gray-`
    - `frontend/src/components/stream/StreamPanel.tsx` contains `border-border` and `bg-card/50`
    - `frontend/src/components/stream/StreamPanel.tsx` contains `uppercase tracking-widest` on Stream label (TYP-01)
    - `frontend/src/components/stream/HistoryStreamPanel.tsx` contains `uppercase tracking-widest` on History label (TYP-01)
    - All stream components that had `font-mono` still have `font-mono` (TYP-02 preserved)
    - `frontend/src/components/stream/AssistantText.tsx` contains `text-foreground`
    - `frontend/src/components/stream/SystemEvent.tsx` contains `border-border`
  </acceptance_criteria>
  <done>All stream components use semantic classes, font-mono preserved on stream output, section labels have uppercase + tracking-widest, zero gray-* remaining</done>
</task>

</tasks>

<verification>
1. `grep -r "gray-" frontend/src/components/ --include="*.tsx" | wc -l` returns 0
2. `grep -q "glow-cyan" frontend/src/components/ui/button.tsx` confirms button glow
3. `grep -r "font-mono" frontend/src/components/stream/ --include="*.tsx" | wc -l` returns > 0 (TYP-02 preserved)
4. `grep -r "uppercase tracking-widest" frontend/src/components/ --include="*.tsx" | wc -l` returns > 0 (TYP-01)
5. `cd frontend && npm run build` succeeds
</verification>

<success_criteria>
- Zero `gray-*` Tailwind classes remain in any file under `frontend/src/components/`
- Button default variant includes `glow-cyan` class (CLR-05)
- All stream output elements retain `font-mono` class (TYP-02)
- Section headings in components have `uppercase tracking-widest` (TYP-01)
- No `focus:ring-blue-500` remains (replaced with semantic `focus:ring-ring`)
- `npm run build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/08-color-system-and-foundation/08-03-SUMMARY.md`
</output>
